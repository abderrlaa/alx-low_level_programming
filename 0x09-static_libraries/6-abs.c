#include"main.h"

/**
 * _abs - fuction that finds the absolute value of a number
 * @a: function parametere
 * Return: a or -a
*/

int _abs(int a)
{
	if (a < 0)
	{
		return (a * -1);
	}
	else
	return (a);
}

